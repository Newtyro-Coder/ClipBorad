[
  "from PyQt5.QtWidgets import QApplication",
  "from PyQt5.QtCore import Qt",
  "import time  # 在文件顶部导入\nfrom PyQt5.QtWidgets import QApplication\nfrom ui.history_window import HistoryWindow\nfrom ui.tray_icon import TrayIcon\nfrom core.listener import ClipboardListener\nfrom pynput import keyboard\nfrom PyQt5.QtCore import QThread, pyqtSignal\n\nclass HotkeyListener(QThread):\n    \"\"\"全局热键监听器（运行在独立线程）\"\"\"\n    activated = pyqtSignal()\n\n    def __init__(self, hotkey='<cmd>+<shift>+v'):\n        super().__init__()\n        self.hotkey = hotkey\n        self.listener = None\n\n    def start(self):\n        def on_activate():\n            print(\"✅ 热键被触发！\")\n            self.activated.emit()  # 通过信号通知主线程\n\n        try:\n            print(f\"🔄 正在注册热键: {self.hotkey}\")\n            self.listener = keyboard.GlobalHotKeys({\n                self.hotkey: on_activate\n            })\n            self.listener.start()  # 非阻塞启动\n            print(\"✅ 热键监听已启动，等待触发...\")\n            # 保持线程运行，直到外部调用 stop()\n            while self.listener.running:\n                time.sleep(0.1)  # 避免 busy loop\n        except Exception as e:\n            print(f\"热键监听启动失败: {e}\")\n\n    def stop(self):\n        if self.listener:\n            print(\"🛑 正在停止热键监听...\")\n            self.listener.stop()\n            self.wait()  # 等待线程结束\n\nclass ClipboardApp(QApplication):\n    def __init__(self, argv):\n        super().__init__(argv)\n        self.setQuitOnLastWindowClosed(False)\n\n        # 创建界面组件\n        self.history_window = HistoryWindow()\n        self.tray = TrayIcon(self)\n        self.tray.show()\n\n        # 连接信号\n        self.history_window.countChanged.connect(self.tray.update_show_action_text)\n\n        # 启动剪贴板监听线程\n        self.clipboard_listener = ClipboardListener()\n        self.clipboard_listener.new_item.connect(self.history_window.add_item)\n        self.clipboard_listener.start()\n\n        # 初始化数量显示\n        self.history_window._update_count_display()\n\n        # 设置全局快捷键（使用 pynput）\n        self.hotkey_listener = HotkeyListener('<ctrl>+<alt>+v')  # Command+Shift+V\n        self.hotkey_listener.activated.connect(self.show_history)\n        self.hotkey_listener.start()\n\n        # 退出清理\n        self.aboutToQuit.connect(self.cleanup)\n\n    def cleanup(self):\n        \"\"\"应用退出前清理资源\"\"\"\n        if hasattr(self, 'clipboard_listener'):\n            self.clipboard_listener.stop()\n            self.clipboard_listener.wait(1000)\n        if hasattr(self, 'hotkey_listener')and self.hotkey_listener.isRunning():\n            self.hotkey_listener.stop()\n\n    def show_history(self):\n        self.history_window.show()\n        self.history_window.raise_()",
  "# 保持线程运行，直到外部调用 stop()\n            while self.listener.running:\n                self.msleep(100)  # 避免 busy loop",
  "from PyQt5.QtWidgets import QApplication\nfrom ui.history_window import HistoryWindow\nfrom ui.tray_icon import TrayIcon\nfrom core.listener import ClipboardListener\nfrom pynput import keyboard\nfrom PyQt5.QtCore import QObject, pyqtSignal\n\nclass HotkeyListener(QObject):\n    \"\"\"全局热键监听器（运行在独立线程）\"\"\"\n    activated = pyqtSignal()\n\n    def __init__(self, hotkey='<cmd>+<shift>+v'):\n        super().__init__()\n        self.hotkey = hotkey\n        self.listener = None\n\n    def start(self):\n        def on_activate():\n            self.activated.emit()  # 通过信号通知主线程\n\n        try:\n            self.listener = keyboard.GlobalHotKeys({\n                self.hotkey: on_activate\n            })\n            self.listener.start()  # 启动监听（非阻塞）\n        except Exception as e:\n            print(f\"热键监听启动失败: {e}\")\n\n    def stop(self):\n        if self.listener:\n            self.listener.stop()\n\nclass ClipboardApp(QApplication):\n    def __init__(self, argv):\n        super().__init__(argv)\n        self.setQuitOnLastWindowClosed(False)\n\n        # 创建界面组件\n        self.history_window = HistoryWindow()\n        self.tray = TrayIcon(self)\n        self.tray.show()\n\n        # 连接信号\n        self.history_window.countChanged.connect(self.tray.update_show_action_text)\n\n        # 启动剪贴板监听线程\n        self.clipboard_listener = ClipboardListener()\n        self.clipboard_listener.new_item.connect(self.history_window.add_item)\n        self.clipboard_listener.start()\n\n        # 初始化数量显示\n        self.history_window._update_count_display()\n\n        # 设置全局快捷键（使用 pynput）\n        self.hotkey_listener = HotkeyListener('<cmd>+<shift>+v')  # Command+Shift+V\n        self.hotkey_listener.activated.connect(self.show_history)\n        self.hotkey_listener.start()\n\n        # 退出清理\n        self.aboutToQuit.connect(self.cleanup)\n\n    def cleanup(self):\n        \"\"\"应用退出前清理资源\"\"\"\n        if hasattr(self, 'clipboard_listener'):\n            self.clipboard_listener.stop()\n            self.clipboard_listener.wait(1000)\n        if hasattr(self, 'hotkey_listener'):\n            self.hotkey_listener.stop()\n\n    def show_history(self):\n        self.history_window.show()\n        self.history_window.raise_()",
  "print(\"✅ 热键监听已启动，等待触发...\")",
  "HESZ@He-MacBook-Pro PythonProject % /usr/local/bin/python3 /Users/HESZ/PythonProject/剪切板/main.py\n🔄 正在注册热键: <ctrl>+<alt>+v\n✅ 热键监听已启动，等待触发...\n热键监听启动失败: module 'time' has no attribute 'msleep'\n2026-03-16 09:05:42.039 Python[70626:2013749] WARNING: Secure coding is automatically enabled for restorable state! However, not on all supported macOS versions of this application. Opt-in to secure coding explicitly by implementing NSApplicationDelegate.applicationSupportsSecureRestorableState:.",
  "HESZ@He-MacBook-Pro PythonProject % /usr/local/bin/python3 /Users/HESZ/PythonProject/剪切板/main.py\n🔄 正在注册热键: <ctrl>+<alt>+v\n✅ 热键监听已启动，等待触发...\n2026-03-16 09:22:36.858 Python[71788:2028833] WARNING: Secure coding is automatically enabled for restorable state! However, not on all supported macOS versions of this application. Opt-in to secure coding explicitly by implementing NSApplicationDelegate.applicationSupportsSecureRestorableState:.",
  "def keyPressEvent(self, event):\n    if event.key() == Qt.Key_Escape:\n        self.close()  # 关闭窗口（隐藏）\n    else:\n        super().keyPressEvent(event)",
  "Traceback (most recent call last):\n  File \"/Users/HESZ/PythonProject/剪切板/ui/tray_icon.py\", line 62, in on_activated\n    def on_activated(self, reason):\n    \nKeyboardInterrupt\nzsh: abort      /usr/local/bin/python3 /Users/HESZ/PythonProject/剪切板/main.py\nHESZ@He-MacBook-Pro PythonProject % /usr/local/bin/python3 /Users/HESZ/PythonProject/剪切板/main.py\n🔄 正在注册热键: <ctrl>+<alt>+v\n✅ 热键监听已启动，等待触发...\n2026-03-16 09:39:37.235 Python[73014:2046042] WARNING: Secure coding is automatically enabled for restorable state! However, not on all supported macOS versions of this application. Opt-in to secure coding explicitly by implementing NSApplicationDelegate.applicationSupportsSecureRestorableState:.\n"
]